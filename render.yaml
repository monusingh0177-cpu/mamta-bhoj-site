# Render "Blueprint" file — lets Render auto-configure this app with zero
# manual setup: no start command to type, no instance type to pick, no env
# vars to remember. Once this repo is on GitHub (public or private), go to
# Render → New → Blueprint → paste the repo URL → Apply. Render reads this
# file and creates the service exactly as specified below, on the Free plan.
#
# To move to a paid plan with a persistent disk later (see README section 3,
# Option B), add a "disk" block here and change PERSIST_DIR to point at its
# mount path — or just make those two changes directly in the Render
# dashboard after the service exists.
services:
  - type: web
    name: mamta-bhoj-site
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: FORCE_HTTPS
        value: "1"
      # Enquiry-email settings (see lib/mailer.js). ENQUIRY_TO_EMAIL has a
      # safe default below; the SMTP_* values are `sync: false` so Render
      # prompts for them in the dashboard instead of storing them here —
      # fill them in with the mailbox's real SMTP credentials after the
      # service is created. Until they're set, enquiries still save
      # normally, they just aren't emailed (see the mailer's own log line).
      - key: ENQUIRY_TO_EMAIL
        value: info@devmamflourishfoods.com
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        sync: false
      - key: SMTP_SECURE
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASSWORD
        sync: false
